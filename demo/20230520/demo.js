/**
* @
*/
const getSun = (a,b,c) => {
  return a + b + c;
}

//console.log(_.defaults({ 'a': 1 }, { 'a': 3, 'b': 2 }))
//const curried = _.curry(getSun);
//console.log(curried(1,2,3));
//console.log(curried(1,2)(3));
//console.log(curried(1)(2)(3));

const reg = /''\s+/g;

const match = (reg, arr) => {
  
}